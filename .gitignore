.DS_Store
node_modules
docs/

# local env files
.env.local
.env.*.local

# site-specific deployment values (hostnames, addresses). deploy.env.example is
# the committed template; the filled-in copy must never be published.
deploy/deploy.env
deploy/runtime.env

# API working directory: database, staged uploads, published media
server/.data/

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw*

dist
# pulled backups: real submitter data, never committed
.backups/

# joi-button docker single-host deploy — never commit local config/secrets
deploy/docker/.env
deploy/docker/runtime.env
deploy/docker/dynamic/*.local.yml
