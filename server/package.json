{
  "name": "joi-button-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Submission, review and catalogue API for joi-button",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "test": "node --test",
    "import-snapshot": "node scripts/import-snapshot.mjs",
    "test:http": "node --test test/http/"
  },
  "dependencies": {
    "@fastify/multipart": "^10.1.0",
    "better-sqlite3": "^12.11.1",
    "fastify": "^5.11.0",
    "music-metadata": "^11.14.0"
  }
}
